const input = document.querySelector('input')
const form = document.querySelector('button')

input.onsubmit = (event) => {
    event.preventDefault()
}
/*
    const value = input.value
    const regex = /\d+/

    if (regex.test(value)) {
        alert("O texto só aceita letras, remova os números")
    } else {
        alert("Enviado")
    }
}
    
*/

alert('teste')